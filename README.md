# coder_buddy
The AI Engineering Project Planner is a multi-agent system built using LangGraph and Groq LLMs. It converts a simple user prompt into a complete engineering project by automatically planning, designing, and generating code files.  The system follows a structured pipeline:
